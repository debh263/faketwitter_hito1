<h1 class="py-4">Tweet</h1>

<% if current_user %>
<%= render "form"%>
    <% @tweets.each do |tweet| %>    
    <div class="card my-4">
    <%= image_tag "#{tweet.image_url}", class: "card-img-top" %>
        <div class="card-bnody">
            <p class="card-text "><%= tweet.content %></p>
        </div>
    </div>
    <% end %>
<%else%>
    <% @tweets.each do |tweet| %>    
    <div class="card my-4">
    <%= image_tag "#{tweet.image_url}", class: "card-img-top" %>
        <div class="card-bnody">
            <p class="card-text "><%= tweet.content %></p>
        </div>
    </div>
    <% end %>
<%end%>





<% @tweets.each do |tweet| %>    
<div class="card my-4">
<%= image_tag "#{tweet.image_url}", class: "card-img-top" %>
    <div class="card-bnody">
        <p class="card-text "><%= tweet.content %></p>
    </div>
</div>
<% end %>

<%= paginate @tweets, param_name: :mostrar%>